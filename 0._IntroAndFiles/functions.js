function doActonWithSomeone(functionRef, name) {
    functionRef(name)
}

const running = (name) => console.log(`${name} is running`);

doActonWithSomeone(running, "Anders")

// implement passing + any action of your choosing 
// bonus task: implement one of them as a one-liner


const youSuckLudvig

const cat = 5;
